<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">
    <t t-inherit="mail.AvatarCardPopover" t-inherit-mode="extension">
        <xpath expr="//a[hasclass('o-mail-avatar-card-tel')]" position="after">
            <span t-if="workLocationName" class="text-muted" data-tooltip="Work Location">
                <i t-if="workLocationType === 'office'" class="me-1 fa fa-fw fa-building-o"/>
                <i t-elif="workLocationType === 'home'" class="me-1 fa fa-fw fa-home"/>
                <i t-else="" class="me-1 fa fa-fw fa-map-marker"/>
                <t t-esc="workLocationName"/>
            </span>
        </xpath>
    </t>

    <t t-name="hr.avatarCardUserInfos">
        <small class="text-muted text-truncate" t-if="jobTitle" t-att-title="jobTitle" t-esc="jobTitle"/>
        <span class="text-muted text-truncate" t-if="departmentName" data-tooltip="Department" t-att-title="departmentName" t-esc="departmentName"/>
    </t>
</templates>
